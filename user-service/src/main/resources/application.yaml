server:
  port: 8082

spring:
  application:
    name: user-service

  config:
    import:
      - "optional:classpath:eureka-client.yaml"

  datasource:
    url: jdbc:postgresql://localhost:5432/mychoice_user
    username: postgres
    password: 1234
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  grpc:
    server:
      port: 9090
